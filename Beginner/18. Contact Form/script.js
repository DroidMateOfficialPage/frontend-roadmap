// Optional UX enhancement (no requirement violation)

// just logs form data like real backend would receive it

document.querySelector("form").addEventListener("submit", (e) => {
    console.log("Form submitted 🚀");
});
